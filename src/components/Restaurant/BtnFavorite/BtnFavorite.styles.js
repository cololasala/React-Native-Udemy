import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    position: "absolute",
    top: 5,
    right: 0,
    backgroundColor: "#fff",
    padding: 5,
    borderRadius: 20
  }
});