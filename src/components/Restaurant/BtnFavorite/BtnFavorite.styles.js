import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    position: "absolute",
    top: 10,
    right: 5,
    backgroundColor: "#fff",
    padding: 5,
    borderRadius: 20
  }
});